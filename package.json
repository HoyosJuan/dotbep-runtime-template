{
  "name": "create-dotbep-runtime",
  "version": "0.1.0",
  "description": "Scaffold a dotbep runtime project",
  "type": "module",
  "bin": {
    "create-dotbep-runtime": "./dist/index.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}