{
  "name": "my-runtime",
  "type": "module",
  "scripts": {
    "pull": "tsx scripts/pull.ts",
    "build:runtime": "tsx scripts/build-runtime.ts",
    "build:lenses": "tsx scripts/build-lenses.ts",
    "deploy:runtime": "npm run build:runtime && tsx scripts/deploy-runtime.ts",
    "deploy:lenses": "npm run build:lenses && tsx scripts/deploy-lenses.ts"
  },
  "dependencies": {
    "@dotbep/core": "0.2.17"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv": "^16.0.0",
    "esbuild": "^0.25.0",
    "lit": "^3.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0",
    "zod": "^4.0.0"
  }
}