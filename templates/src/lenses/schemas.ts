// import { z } from 'zod'

// Define Zod schemas for your custom lenses here.
// These are used to validate data before passing it to a lens component.
//
// Example:
// export const myLensSchema = z.array(z.object({
//   label: z.string(),
//   value: z.number(),
// }))
//
// export type MyLensItem = z.infer<typeof myLensSchema>[number]
